export type goodsType = {
    id: number;
    name: string;
    price: string;
    imageURL: string;
    type?: string,
    series: string;
    rating: number,
    category: string,
    color: string;
    brand: string;
}